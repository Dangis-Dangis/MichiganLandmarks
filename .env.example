# Copy this file to .env and fill in values. .env is gitignored.
# Get a free key: https://www.nps.gov/subjects/developer/get-started.htm
NPS_API_KEY=

# JDK 21 root (not the bin/ folder). Needed for CLI Gradle APK builds.
# Typical Android Studio install on Windows:
# JAVA_HOME=C:\Program Files\Android\Android Studio\jbr
JAVA_HOME=

# Leftover museum Nominatim geocoding is a CLI flag, not an env var:
#   python -m pipeline.run --geocode-museums
# MUSEUM_GEOCODE=1 is still honored if already set in a local .env.
